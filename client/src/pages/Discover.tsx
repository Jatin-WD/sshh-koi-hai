import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { api } from "../lib/api";
import SafetyActions from "../components/SafetyActions";

type FindProfile = { id: string; displayName: string; age: number; city: string | null; bio: string | null; interests: string[]; profileImages: string[]; primaryImageIndex: number; onlineStatus?: boolean; relationshipIntent?: string | null };
type ProfilePreference = { genderPreference?: string | null; agePreferenceMin?: number | null; agePreferenceMax?: number | null };

export default function Discover() {
  const [profiles, setProfiles] = useState<FindProfile[]>([]); const [hasMore, setHasMore] = useState(false); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [preference, setPreference] = useState<ProfilePreference>({}); const [showAll, setShowAll] = useState(false); const [interestState, setInterestState] = useState<Record<string, "sending" | "sent">>({}); const [actionError, setActionError] = useState(""); const [membershipPrompt, setMembershipPrompt] = useState(false);
  async function loadProfiles(all = showAll) { setLoading(true); setError(""); try { const me = await api<{ profile: ProfilePreference }>("/profile/me"); setPreference(me.profile); const query = new URLSearchParams({ page: "1", pageSize: "24" }); if (!all && me.profile.genderPreference && me.profile.genderPreference !== "PREFER_NOT_TO_SAY") query.set("gender", me.profile.genderPreference); if (!all && me.profile.agePreferenceMin) query.set("minAge", String(me.profile.agePreferenceMin)); if (!all && me.profile.agePreferenceMax) query.set("maxAge", String(me.profile.agePreferenceMax)); const result = await api<{ profiles: FindProfile[]; pagination: { hasMore: boolean } }>(`/discover?${query}`); setProfiles(result.profiles); setHasMore(result.pagination.hasMore); } catch (e) { setError(e instanceof Error ? e.message : "Unable to load profiles"); } finally { setLoading(false); } }
  useEffect(() => { void loadProfiles(); }, []);
  async function sendInterest(receiverId: string) { setInterestState((current) => ({ ...current, [receiverId]: "sending" })); setActionError(""); setMembershipPrompt(false); try { await api("/interests", { method: "POST", body: JSON.stringify({ receiverId }) }); setInterestState((current) => ({ ...current, [receiverId]: "sent" })); } catch (e) { setInterestState((current) => { const next = { ...current }; delete next[receiverId]; return next; }); const message = e instanceof Error ? e.message : "Unable to send interest"; if (message.toLowerCase().includes("membership")) setMembershipPrompt(true); else setActionError(message); } }
  const genderLabel = showAll ? "everyone" : preference.genderPreference === "MALE" ? "men" : preference.genderPreference === "FEMALE" ? "women" : "people";
  return <section className="mx-auto max-w-7xl px-5 py-10 sm:px-6 lg:px-10 lg:py-12"><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="eyebrow text-burgundy/65">Find people</p><h1 className="mt-3 font-display text-4xl sm:text-5xl">Someone you may like.</h1><p className="mt-4 max-w-xl text-sm leading-6 text-charcoal/60">Real profiles, shared intentions, and a quieter way to find a good conversation.</p></div><span className="rounded-full border border-charcoal/10 px-4 py-2 text-xs uppercase tracking-[0.18em] text-charcoal/50">Showing {genderLabel}</span></div><div className="mt-8 flex flex-wrap items-center justify-between gap-4 rounded-3xl border border-burgundy/10 bg-white/70 px-5 py-4 shadow-sm"><div><p className="text-sm font-semibold text-burgundy">{showAll ? "Showing all available people" : "Matched to your preferences"}</p><p className="mt-1 text-xs text-charcoal/55">{showAll ? "Your privacy and safety settings still apply." : "Your profile preferences shape this list, so there is nothing extra to fill in here."}</p></div><Link to="/app/profile" className="rounded-full border border-charcoal/15 px-4 py-2.5 text-sm font-semibold text-burgundy">Change preferences</Link></div>{membershipPrompt && <div className="mt-6 flex flex-wrap items-center justify-between gap-4 rounded-3xl border border-gold/40 bg-gold/10 px-5 py-4"><div><p className="text-sm font-semibold text-burgundy">Ready to connect privately?</p><p className="mt-1 text-xs text-charcoal/60">Subscribe to send unlimited interests and start private conversations.</p></div><Link to="/membership" className="rounded-full bg-burgundy px-4 py-2.5 text-sm font-semibold text-cream">Explore membership</Link></div>}{actionError && <p role="alert" className="mt-6 rounded-xl bg-red-50 p-4 text-sm text-red-800">{actionError}</p>}{error && <div className="mt-8 rounded-2xl bg-red-50 p-5 text-sm text-red-800"><p>{error}</p><button onClick={() => void loadProfiles()} className="mt-3 font-semibold underline">Try again</button></div>}{!loading && !error && profiles.length === 0 && <div className="mt-10 rounded-3xl border border-charcoal/10 bg-white/60 p-10 text-center sm:p-12"><h2 className="font-display text-3xl">{showAll ? "No profiles are available yet." : "No one fits this view yet."}</h2><p className="mt-3 text-sm text-charcoal/60">{showAll ? "New conversations take time. Check back soon or invite someone you trust." : "Try widening your view before changing your profile."}</p>{!showAll && <button onClick={() => { setShowAll(true); void loadProfiles(true); }} className="mt-6 rounded-full bg-burgundy px-5 py-3 text-sm font-semibold text-cream">Show all available people</button>}<Link to="/app/profile" className="ml-3 mt-6 inline-flex rounded-full border border-charcoal/15 px-5 py-3 text-sm font-semibold text-burgundy">Update preferences</Link></div>}<div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{loading ? [1, 2, 3].map((item) => <div key={item} className="skeleton h-[30rem] rounded-3xl" />) : profiles.map((profile) => <article key={profile.id} className="overflow-hidden rounded-3xl border border-charcoal/10 bg-white/75 shadow-sm transition hover:-translate-y-1 hover:shadow-soft"><Link to={`/app/profile/${profile.id}`} className="block"><div className="relative aspect-[4/3] bg-gradient-to-br from-rose/70 via-burgundy to-plum">{profile.profileImages[profile.primaryImageIndex] && <img src={profile.profileImages[profile.primaryImageIndex]} alt={`${profile.displayName}'s profile`} loading="lazy" className="h-full w-full object-cover" />}{profile.onlineStatus !== undefined && <span className="absolute left-4 top-4 rounded-full bg-charcoal/65 px-3 py-1.5 text-xs text-cream">{profile.onlineStatus ? "Online" : "Member"}</span>}</div><div className="p-6"><div className="flex items-start justify-between gap-3"><div><h2 className="font-display text-3xl">{profile.displayName}, {profile.age}</h2><p className="mt-2 text-sm text-charcoal/55">{profile.city ?? "Location private"}</p></div><span className="pt-1 text-sm font-semibold text-burgundy">View →</span></div><p className="mt-4 line-clamp-3 text-sm leading-6 text-charcoal/65">{profile.bio || "A member who is leaving room for a good conversation."}</p>{profile.relationshipIntent && <p className="mt-4 text-xs font-semibold text-burgundy">{profile.relationshipIntent}</p>}<div className="mt-5 flex flex-wrap gap-2">{profile.interests.slice(0, 4).map((interest) => <span key={interest} className="rounded-full bg-cream px-3 py-1 text-xs text-burgundy">{interest}</span>)}</div></div></Link><div className="px-6 pb-6"><button disabled={interestState[profile.id] !== undefined} onClick={() => void sendInterest(profile.id)} className={`w-full rounded-full px-4 py-2.5 text-sm font-semibold ${interestState[profile.id] === "sent" ? "bg-green-100 text-green-800" : "bg-burgundy text-cream disabled:opacity-60"}`}>{interestState[profile.id] === "sending" ? "Sending..." : interestState[profile.id] === "sent" ? "Interest sent" : "Send interest"}</button><SafetyActions userId={profile.id} displayName={profile.displayName} onBlocked={() => setProfiles((current) => current.filter((item) => item.id !== profile.id))}/></div></article>)}</div>{!loading && hasMore && <p className="mt-10 text-center text-sm text-charcoal/55">More profiles are waiting as your circle grows.</p>}</section>;
}
