import { Link, NavLink, Outlet } from "react-router-dom";
import Brand from "./Brand";

const links = [["/admin", "Dashboard"], ["/admin/users", "Users"], ["/admin/subscriptions", "Subscriptions"], ["/admin/payments", "Payments"], ["/admin/plans", "Plans"], ["/admin/reports", "Reports"], ["/admin/settings", "Settings"]];
export default function AdminLayout() {
  return <div className="mx-auto flex max-w-7xl gap-6 px-4 py-8 sm:px-6"><aside className="hidden w-52 shrink-0 rounded-3xl bg-charcoal p-4 text-cream md:block"><Link to="/admin" aria-label="Sshh... Koi Hai? admin home" className="block border-b border-cream/10 px-3 pb-5"><Brand className="h-12 w-full rounded-lg bg-cream p-1 object-contain" /></Link><nav className="mt-4 space-y-1">{links.map(([to, label]) => <NavLink key={to} to={to} end={to === "/admin"} className={({ isActive }) => `block rounded-xl px-3 py-2 text-sm ${isActive ? "bg-cream text-charcoal" : "text-cream/65 hover:bg-white/10 hover:text-cream"}`}>{label}</NavLink>)}</nav></aside><main className="min-w-0 flex-1"><div className="mb-5 flex items-center justify-between"><div><p className="text-xs uppercase tracking-[0.25em] text-burgundy/60">Private operations</p><h1 className="font-display text-3xl">Admin workspace</h1></div><Link to="/account" className="text-sm text-burgundy">Back to account</Link></div><div className="mb-5 flex gap-2 overflow-x-auto md:hidden">{links.map(([to, label]) => <NavLink key={to} to={to} end={to === "/admin"} className="whitespace-nowrap rounded-full border border-charcoal/15 px-3 py-2 text-xs">{label}</NavLink>)}</div><Outlet /></main></div>;
}
