import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { api } from "../lib/api";
import { useAuth } from "../context/AuthContext";

type Notice = { id: string; title: string; body: string; readAt: string | null };
export default function NotificationBell() { const { user } = useAuth(); const [open, setOpen] = useState(false); const [items, setItems] = useState<Notice[]>([]); const [unread, setUnread] = useState(0); useEffect(() => { if (user) api<{ notifications: Notice[]; unreadCount: number }>("/notifications?pageSize=6").then((r) => { setItems(r.notifications); setUnread(r.unreadCount); }).catch(() => undefined); }, [user]); if (!user) return null; async function markRead(id: string) { await api(`/notifications/${id}/read`, { method: "PATCH" }); setItems(items.map((item) => item.id === id ? { ...item, readAt: new Date().toISOString() } : item)); setUnread(Math.max(0, unread - 1)); } return <div className="relative"><button aria-label="Notifications" onClick={() => setOpen(!open)} className="relative rounded-full border border-charcoal/15 px-3 py-2 text-sm">Bell{unread > 0 && <span className="absolute -right-1 -top-1 min-w-5 rounded-full bg-burgundy px-1 text-center text-[10px] leading-5 text-cream">{unread > 9 ? "9+" : unread}</span>}</button>{open && <div className="absolute right-0 z-30 mt-3 w-80 rounded-2xl border border-charcoal/10 bg-cream p-3 shadow-soft"><div className="flex justify-between px-2 pb-2"><strong>Notifications</strong><button className="text-xs text-burgundy" onClick={async () => { await api("/notifications/read-all", { method: "POST" }); setItems(items.map((item) => ({ ...item, readAt: new Date().toISOString() }))); setUnread(0); }}>Mark all read</button></div>{items.length ? items.map((item) => <button key={item.id} onClick={() => item.readAt ? undefined : markRead(item.id)} className={`block w-full rounded-xl p-2 text-left text-sm ${item.readAt ? "opacity-60" : "bg-white"}`}><strong>{item.title}</strong><span className="mt-1 block text-xs text-charcoal/65">{item.body}</span></button>) : <p className="p-3 text-sm text-charcoal/60">Nothing new.</p>}<Link to="/notifications" onClick={() => setOpen(false)} className="mt-2 block border-t border-charcoal/10 p-2 pt-3 text-center text-xs font-semibold text-burgundy">View all notifications</Link></div>}</div>; }
