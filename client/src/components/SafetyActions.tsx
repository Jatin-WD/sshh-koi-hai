import { useState, type FormEvent } from "react";
import { api } from "../lib/api";

const categories = ["Harassment", "Fake Profile", "Spam", "Inappropriate Content", "Scam/Fraud", "Impersonation", "Other"];
export default function SafetyActions({ userId, displayName, onBlocked }: { userId: string; displayName: string; onBlocked: () => void }) { const [modal, setModal] = useState<"block" | "report" | null>(null); const [category, setCategory] = useState(categories[0]); const [details, setDetails] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); setError(""); try { if (modal === "block") { await api(`/users/${userId}/block`, { method: "POST", body: JSON.stringify({}) }); onBlocked(); } else if (modal === "report") { await api(`/users/${userId}/report`, { method: "POST", body: JSON.stringify({ category, ...(details ? { details } : {}) }) }); setModal(null); setDetails(""); } } catch (e) { setError(e instanceof Error ? e.message : "Safety action failed"); } finally { setBusy(false); } } return <><div className="mt-5 flex gap-3"><button onClick={() => setModal("block")} className="text-xs text-charcoal/50 underline">Block</button><button onClick={() => setModal("report")} className="text-xs text-charcoal/50 underline">Report</button></div>{modal && <div className="fixed inset-0 z-50 flex items-center justify-center bg-charcoal/50 p-5" role="dialog" aria-modal="true"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-cream p-7 shadow-soft"><p className="text-xs uppercase tracking-[0.25em] text-burgundy/65">Safety first</p><h2 className="mt-3 font-display text-3xl">{modal === "block" ? `Block ${displayName}?` : `Report ${displayName}`}</h2>{modal === "block" ? <p className="mt-4 text-sm leading-6 text-charcoal/65">They will disappear from discovery and will not be able to start new interactions or chat with you. Existing conversation history is retained privately.</p> : <><label className="mt-5 block text-sm">Reason<select value={category} onChange={(event) => setCategory(event.target.value)} className="profile-input">{categories.map((item) => <option key={item}>{item}</option>)}</select></label><label className="mt-4 block text-sm">Details <span className="text-charcoal/45">optional</span><textarea value={details} onChange={(event) => setDetails(event.target.value)} rows={4} maxLength={2000} className="profile-input" placeholder="Tell us what happened." /></label></>} {error && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-800">{error}</p>}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setModal(null)} className="rounded-full border border-charcoal/15 px-4 py-2.5 text-sm">Cancel</button><button disabled={busy} className="rounded-full bg-burgundy px-4 py-2.5 text-sm font-semibold text-cream">{busy ? "Sending..." : modal === "block" ? "Block user" : "Send report"}</button></div></form></div>}</>; }
